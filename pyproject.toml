[project]
name = "backlogia"
version = "0.1.0"
description = "Self-hosted game library aggregator"
requires-python = ">=3.11"
dependencies = [
    # Steam
    "requests",
    # Epic
    "legendary-gl",
    # Amazon (nile, fetched+patched by scripts/fetch-nile.sh)
    "nile",
    # Web interface
    "fastapi",
    "uvicorn[standard]",
    "python-multipart",
    "jinja2",
    # IGDB integration
    "python-dotenv",
    # Metacritic scraping
    "beautifulsoup4",
    # Authentication (optional)
    "bcrypt",
    "itsdangerous",
]

[dependency-groups]
dev = [
    "pytest",
    "httpx",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.uv.workspace]
members = [
    "vendor/nile",
]

[tool.uv.sources]
nile = { workspace = true }
